version: "3.9"

services:
  hermes:
    image: hermes
    container_name: hermes_test
    environment:
      - HERMES_LOG=info
    volumes:
      - ./var:/usr/var/hermes
      - ./etc:/etc/olympus/hermes
    ports:
      - 3030-3032:3030-3032
